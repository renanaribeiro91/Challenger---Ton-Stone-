import { StyleSheet } from "react-native";

export default StyleSheet.create({
  form: { width: "100%" },
  input: {
    height: 45,
    fontSize: 14,
    borderWidth: 1,
    borderColor: "#ddd",
    paddingHorizontal: 10,
    borderRadius: 5,
    marginBottom: 10,
    color: "#333",
    backgroundColor: "#fff",
  },
});
